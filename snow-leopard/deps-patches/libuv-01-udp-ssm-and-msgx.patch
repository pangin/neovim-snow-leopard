--- a/src/unix/udp.c
+++ b/src/unix/udp.c
@@ -20,6 +20,12 @@
  */
 
 #include "uv.h"
+#if defined(__APPLE__)
+/* Mac OS X 10.6 (Snow Leopard): recvmsg_x/sendmsg_x arrived in 10.10.
+   Gate their use on the deployment target so older systems keep the
+   generic recvmsg/sendmsg path. */
+# include <AvailabilityMacros.h>
+#endif
 #include "internal.h"
 
 #include <assert.h>
@@ -152,7 +158,7 @@
 }
 
 static int uv__udp_recvmmsg(uv_udp_t* handle, uv_buf_t* buf) {
-#if defined(__linux__) || defined(__FreeBSD__) || defined(__APPLE__)
+#if defined(__linux__) || defined(__FreeBSD__) || defined(__APPLE__) && MAC_OS_X_VERSION_MIN_REQUIRED >= 101000
   struct sockaddr_in6 peers[20];
   struct iovec iov[ARRAY_SIZE(peers)];
   struct mmsghdr msgs[ARRAY_SIZE(peers)];
@@ -180,7 +186,7 @@
     msgs[k].msg_len = 0;
   }
 
-#if defined(__APPLE__)
+#if defined(__APPLE__) && MAC_OS_X_VERSION_MIN_REQUIRED >= 101000
   do
     nread = recvmsg_x(handle->io_watcher.fd, msgs, chunks, MSG_DONTWAIT);
   while (nread == -1 && errno == EINTR);
@@ -754,7 +760,7 @@
     !defined(__ANDROID__) &&                                        \
     !defined(__DragonFly__) &&                                      \
     !defined(__QNX__) &&                                            \
-    !defined(__GNU__)
+    !defined(__GNU__) && !defined(UV_NO_SSM)
 static int uv__udp_set_source_membership4(uv_udp_t* handle,
                                           const struct sockaddr_in* multicast_addr,
                                           const char* interface_addr,
@@ -945,7 +951,7 @@
     !defined(__ANDROID__) &&                                        \
     !defined(__DragonFly__) &&                                      \
     !defined(__QNX__) &&                                            \
-    !defined(__GNU__)
+    !defined(__GNU__) && !defined(UV_NO_SSM)
   int err;
   union uv__sockaddr mcast_addr;
   union uv__sockaddr src_addr;
@@ -1298,7 +1304,7 @@
   r = 0;
   nsent = 0;
 
-#if defined(__linux__) || defined(__FreeBSD__) || defined(__APPLE__)
+#if defined(__linux__) || defined(__FreeBSD__) || defined(__APPLE__) && MAC_OS_X_VERSION_MIN_REQUIRED >= 101000
   if (count > 1) {
     for (i = 0; i < count; /*empty*/) {
       struct mmsghdr m[20];
@@ -1309,7 +1315,7 @@
           goto exit;
 
       do
-#if defined(__APPLE__)
+#if defined(__APPLE__) && MAC_OS_X_VERSION_MIN_REQUIRED >= 101000
         r = sendmsg_x(fd, m, n, MSG_DONTWAIT);
 #else
         r = sendmmsg(fd, m, n, 0);
